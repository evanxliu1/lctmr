% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lctm_auto.R
\name{lctm_auto}
\alias{lctm_auto}
\title{Automated LCTM Analysis}
\usage{
lctm_auto(
  data,
  outcome,
  time_var,
  id_var,
  k_range = 2:7,
  models = c("F", "E"),
  adequacy_thresholds = list(appa = 0.7, occ = 5, entropy = 0.5),
  try_splines = TRUE,
  try_linear = TRUE,
  degree = 2,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame containing longitudinal data.}

\item{outcome}{Character string naming the outcome variable.}

\item{time_var}{Character string naming the time variable.}

\item{id_var}{Character string naming the subject ID variable.}

\item{k_range}{Integer vector of K values to search (default 2:7).
K=1 is excluded because LCTM requires at least 2 classes.}

\item{models}{Character vector of model types to try (default c("E", "F")).}

\item{adequacy_thresholds}{List of adequacy thresholds:
\describe{
\item{appa}{Minimum APPA (default 0.70)}
\item{occ}{Minimum OCC (default 5.0)}
\item{entropy}{Minimum relative entropy (default 0.5)}
}}

\item{try_splines}{Logical; if TRUE, tries adding splines when adequacy fails.}

\item{try_linear}{Logical; if TRUE, tries linear models when adequacy fails.}

\item{degree}{Polynomial degree: 1 = linear, 2 = quadratic (default).}

\item{verbose}{Logical; if TRUE, prints progress messages.}
}
\value{
An \code{lctm_result} object containing:
\describe{
\item{best_model}{The best lctm_model object that passed adequacy, or NULL}
\item{best_k}{Number of classes in best model}
\item{best_model_type}{Model type of best model ("E" or "F")}
\item{adequacy}{lctm_adequacy object for best model}
\item{bic_table}{BIC comparison table from setup}
\item{all_models}{List of all fitted models (optional)}
\item{class_assignments}{Data frame with class assignments for best model}
\item{search_history}{Data frame documenting the search process}
}
}
\description{
Performs a complete latent class trajectory modeling analysis automatically,
selecting the best model that meets adequacy criteria.
}
\details{
This function automates the 5-step LCTM workflow:
\enumerate{
\item \strong{Setup:} Validates data and compares BIC across K values
\item \strong{Search:} For each K (in BIC order):
\itemize{
\item Tries Model F, then Model E
\item If adequacy fails and \code{try_splines = TRUE}, tries with splines
\item If adequacy fails and \code{try_linear = TRUE}, tries linear model
}
\item \strong{Selection:} Returns first model that passes all adequacy criteria
}

If no model passes adequacy, returns result with \code{best_model = NULL}.
}
\examples{
\dontrun{
data(sample_growth)

# Fully automated analysis
result <- lctm_auto(sample_growth,
                    outcome = "weight_raw",
                    time_var = "anthroage",
                    id_var = "childid")

# View results
print(result)
plot(result)

# Access components
result$best_k
result$adequacy
}

}
