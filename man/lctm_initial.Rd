% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lctm_initial.R
\name{lctm_initial}
\alias{lctm_initial}
\title{Fit Initial LCTM Model with Diagnostic Plots}
\usage{
lctm_initial(
  data,
  outcome = NULL,
  time_var = NULL,
  id_var = NULL,
  k = 2,
  degree = 2,
  knots = NULL,
  spline_degree = NULL,
  sex_var = NULL,
  save_pdf = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{An \code{lctm_cleaned} object from \code{\link[=lctm_clean]{lctm_clean()}}, or a raw data frame.}

\item{outcome}{Character string naming the outcome variable. Required if
\code{data} is a raw data frame; ignored if \code{data} is an \code{lctm_cleaned} object.}

\item{time_var}{Character string naming the time variable.}

\item{id_var}{Character string naming the subject ID variable.}

\item{k}{Integer; number of latent classes (default 2).}

\item{degree}{Polynomial degree: 1 = linear, 2 = quadratic (default), 3 = cubic.}

\item{knots}{Numeric vector of knot positions for splines. If provided,
spline terms are added to the fixed and mixture formulas. The knots are
placed at the specified ages/time values (e.g., \code{knots = c(6, 12, 24)} for
knots at 6, 12, and 24 months). When \code{knots} are supplied the spline defines
the trajectory shape, so \code{degree} is automatically set to 1 (with a message).}

\item{spline_degree}{Polynomial degree of the spline pieces when \code{knots} is
supplied: \code{2} builds a quadratic B-spline via \code{splines::bs(degree = 2)}
(piecewise quadratic, C1 continuity, fewer parameters per class); \code{3} builds
a natural cubic spline via \code{splines::ns()} (piecewise cubic, C2 continuity).
If NULL (default), a natural cubic spline is used. Supplying it without
\code{knots} is ignored (with a warning), since spline order only matters for a
spline.}

\item{sex_var}{Character string naming the sex variable (optional). Used for
faceting spaghetti and loess plots.}

\item{save_pdf}{Character string; file path to save plots as multi-page PDF.
If NULL (default), no PDF is saved. The PDF includes all diagnostic plots
plus a reference page with example residual patterns.}

\item{verbose}{Logical; if TRUE, prints progress messages.}
}
\value{
An \code{lctm_initial} object containing:
\describe{
\item{data}{The data frame used}
\item{outcome}{Name of outcome variable}
\item{time_var}{Name of time variable}
\item{id_var}{Name of ID variable}
\item{sex_var}{Name of sex variable (or NULL)}
\item{degree}{Polynomial degree}
\item{k}{Number of classes}
\item{knots}{Knot positions (or NULL)}
\item{spline_degree}{Spline piece degree used when knots are supplied}
\item{initial_model}{The fitted hlme object (random = ~1)}
\item{base_model}{The ng=1 base model for starting values}
\item{plots}{Named list of ggplot objects (spaghetti, loess, residuals, guide)}
}
}
\description{
Fits an initial latent class trajectory model using random intercept only
(no random slopes) and generates diagnostic plots to guide the choice of
random effects structure for refinement.
}
\details{
This function fits an initial model with random intercept only. The residual
plots help determine what random effects structure to use in \code{\link[=lctm_refine]{lctm_refine()}}:

\describe{
\item{Horizontal band}{Random intercept only is sufficient}
\item{Diagonal/linear trend}{Add linear random slope}
\item{Curved pattern}{Add quadratic random effect}
\item{S-shaped pattern}{Consider cubic terms or splines}
}

This is the second step of the new LCTM workflow:

\code{lctm_clean() -> lctm_initial() -> [user reviews] -> lctm_refine()}
}
\examples{
\dontrun{
data(sample_growth)
cleaned <- lctm_clean(sample_growth, "weight_raw", "anthroage", "childid")
init <- lctm_initial(cleaned, k = 2, degree = 2)
plot(init)

# With cubic model
init_cubic <- lctm_initial(cleaned, k = 2, degree = 3)

# With natural cubic spline knots at specific ages
init_spline <- lctm_initial(cleaned, k = 2, degree = 1, knots = c(6, 12, 24))

# With a quadratic (piecewise) spline instead of cubic
init_q <- lctm_initial(cleaned, k = 2, degree = 1, knots = c(6, 12, 24),
                       spline_degree = 2)
}

}
