% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lctm_plot.R
\name{lctm_plot_trajectories}
\alias{lctm_plot_trajectories}
\title{Plot LCTM Trajectories (Step 5)}
\usage{
lctm_plot_trajectories(
  model,
  type = "mean",
  ci = TRUE,
  time_range = NULL,
  n_points = 100,
  colors = NULL,
  alpha = 0.3
)
}
\arguments{
\item{model}{An \code{lctm_model} object from \code{\link[=lctm_fit]{lctm_fit()}}.}

\item{type}{Character; type of plot - "mean" for mean trajectories,
"spaghetti" for individual trajectories, or "both" for both plots.}

\item{ci}{Logical; if TRUE, adds confidence intervals to mean trajectory plot.
Currently uses approximate intervals based on class-specific variation.}

\item{time_range}{Numeric vector of length 2 specifying the time range to plot.
If NULL (default), uses the range of observed time values.}

\item{n_points}{Integer; number of points for mean trajectory prediction (default 100).}

\item{colors}{Character vector of colors for each class. If NULL, uses default palette.}

\item{alpha}{Numeric; transparency for spaghetti plot lines (default 0.3).}
}
\value{
A ggplot object (if type is "mean" or "spaghetti") or a list of
ggplot objects (if type is "both").
}
\description{
Creates visualizations of fitted latent class trajectories.
}
\details{
This function performs Step 5 of the LCTM workflow - visualizing the results.

\strong{Plot Types:}
\describe{
\item{mean}{Shows the predicted mean trajectory for each class with optional
confidence intervals.}
\item{spaghetti}{Shows individual trajectories colored by assigned class,
useful for seeing within-class variation.}
\item{both}{Returns a list with both plots.}
}
}
\examples{
\dontrun{
data(sample_growth)
setup <- lctm_setup(sample_growth, "weight_raw", "anthroage", "childid")
model <- lctm_fit(setup, k = 3, model = "F")

# Mean trajectory plot
lctm_plot_trajectories(model, type = "mean")

# Spaghetti plot
lctm_plot_trajectories(model, type = "spaghetti")

# Both plots
plots <- lctm_plot_trajectories(model, type = "both")
plots$mean
plots$spaghetti
}

}
